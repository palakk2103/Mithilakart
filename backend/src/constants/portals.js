const PORTALS = {
  CUSTOMER: 'customer',
  SELLER: 'seller',
  ADMIN: 'admin',
  DELIVERY: 'delivery',
};

const PORTAL_VALUES = Object.values(PORTALS);

module.exports = {
  PORTALS,
  PORTAL_VALUES,
};
